# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: dfbf0f27-88b9-4a86-b180-b33923eef211


# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://127.0.0.1:7073
    variables:
      catalog: databricksspotifyproject
      schema: dev
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/prasunjeetdutta007@hotmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/prasunjeetdutta007@hotmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksspotifyproject
      schema: prod
    permissions:
      - user_name: prasunjeetdutta007@hotmail.com
        level: CAN_MANAGE
